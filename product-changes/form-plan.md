### Booking form plan

1. Create a booking form with the following fields:
  a. Name
  b. Email
  c. Phone
2. Add a Calendar component that allows the user to select a date
  a. Calendar should be allowed to schedule 2 months in advance
3. Add a Time component that allows the user to select a time
4. Before selecting a date, an api call is made to verify if the selected date is available
  a. After selecting a date, the list of available times are shown